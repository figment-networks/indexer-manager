name: Cosmos Indexer

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    name: Upload Release Asset
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Setup
        uses: actions/setup-go@v2
        with:
          go-version: 1.14

      - name: Build manager binary
        run: make build-manager-w-scheduler

      - name: Build manager migration
        run: make build-manager-migration

      - name: Build cosmos binary
        run: make build-cosmos

      - name: Pack messages
        run: |
          zip --junk-paths cosmos-indexer manager_migration_bin manager_bin worker_cosmos_bin

      - name: Set ref vars
        id: refs
        run: |
          echo ::set-output name=SOURCE_NAME::${GITHUB_REF#refs/*/}
          echo ::set-output name=SOURCE_BRANCH::${GITHUB_REF#refs/heads/}
          echo ::set-output name=SOURCE_TAG::${GITHUB_REF#refs/tags/}

      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ steps.refs.outputs.SOURCE_TAG }}
          release_name: "Release ${{ steps.refs.outputs.SOURCE_TAG }}"
          draft: false
          prerelease: false

      - name: Upload Release Asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./cosmos-indexer.zip
          asset_name: cosmos-indexer.zip
          asset_content_type: application/zip
